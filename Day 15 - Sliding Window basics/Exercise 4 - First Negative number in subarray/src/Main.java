/**
 * Purpose: Using a sliding window, output the first negative number in each window of size k
 *
 * My thought process (done with GPT) tonight happened to be:
 * Sliding window state:
 * - Exercise 1-3: store a number (sum/average)
 * - Exercise 4: store candidates + their positions
 * - Need FIFO behaviour -> queue
 * */

public static int[] slidingWindow(int[] arr, int k) {
    int[] negatives = new int[arr.length - k + 1];
    int temporary;
    int i = 0;
    return negatives;
}

public static void main(String[] args) {

}